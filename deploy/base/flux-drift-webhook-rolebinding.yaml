apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: flux-drift-webhook-leader-election
  namespace: flux-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: flux-drift-webhook-leader-election
subjects:
  - kind: ServiceAccount
    name: flux-drift-webhook
    namespace: flux-system
